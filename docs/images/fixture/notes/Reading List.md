# Reading List

No dates in here — this note is in scope for a vault-wide run but has nothing
to replace. That is the point: the preview shows which files are affected, and
this one is not among them.
